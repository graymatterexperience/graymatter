// so I had it working ONCE... now it does not work. when I commented out this code
// then it starting working. now I did a couple of things.. and wamp.. doesn't work agian
//$(document).ready(function(){

//$('.modal').modal({
    //dismissible: true
//});
  //console.log('this needs to happen')
  //$('.modal').modal();
//});
